package core;

/**
 * Describes the different outcomes when creating a {@link core.User} with a username and password.
 */
public enum UserValidation {
  OK,
  ILLEGAL_INPUT_LENGTH,
  ILLEGAL_INPUT
}
